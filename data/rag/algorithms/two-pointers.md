# 双指针

## 概述

双指针（Two Pointers）是一种使用两个索引变量同时遍历数组或链表的算法技巧，通过控制两个指针的移动方向和速度，将原本需要 $O(n^2)$ 的暴力枚举降低至 $O(n)$ 或 $O(n \log n)$ 的时间复杂度。两个指针可以从同一端出发（快慢指针），也可以从两端向中间收拢（对撞指针），具体选择取决于问题的约束条件。

双指针技巧最早在算法竞赛领域被系统总结，与荷兰国旗问题（Dijkstra，1976年提出）密切相关——该问题的三路划分解法正是双指针思想的经典体现。Floyd 判圈算法（Floyd's Cycle Detection Algorithm）由 Robert W. Floyd 于1967年独立提出，是快慢指针在链表场景下的奠基性工作，该算法又称"龟兔赛跑算法"（Tortoise and Hare Algorithm）。LeetCode 平台上标注为"Two Pointers"的题目超过 150 道，是面试高频考点之一，也是滑动窗口、快速排序分区等高级技巧的直接前身。

双指针的核心价值在于利用数据的有序性或特定约束消除冗余计算。以在长度为 $n$ 的有序数组中寻找两数之和为例：暴力枚举需要检查所有 $\binom{n}{2} = \frac{n(n-1)}{2}$ 个候选对，而对撞指针每一步都能根据当前和与目标值的大小关系确定性地排除至少一个元素，使总操作次数严格不超过 $n$ 次，时间复杂度从 $O(n^2)$ 压缩至 $O(n)$，且额外空间消耗为 $O(1)$。

思考一个问题：**如果数组是循环有序的（即数组经过若干次旋转），对撞指针还能直接适用吗？如果不能，应当如何改造才能保留 $O(n)$ 的时间复杂度？**

## 核心原理

### 对撞指针（左右指针）

对撞指针将左指针 `left` 初始化为 0，右指针 `right` 初始化为 `n-1`，两者向中间移动直到相遇。其正确性依赖于**单调性**：当 `arr[left] + arr[right] > target` 时，右移左指针只会让和变大，因此必须左移右指针；反之右移左指针。每次操作必然使搜索空间缩小至少 1，故循环至多执行 $n$ 次。

形式化描述如下：设有序数组 $A[0..n-1]$，目标值为 $T$。对撞指针在任意时刻维护不变式：若满足条件的下标对 $(i, j)$ 存在，则必有 $\text{left} \leq i < j \leq \text{right}$。每次移动均保持此不变式成立，当 `left == right` 时搜索结束。

典型场景：**两数之和（有序数组，LeetCode 167）**。给定升序数组 `nums` 和目标值 `target`，对撞指针的判断逻辑为：
- `nums[left] + nums[right] == target`：返回 `[left+1, right+1]`（题目下标从1开始）
- `nums[left] + nums[right] < target`：`left++`，因为 `nums[left]` 与任何 `right` 以右的元素配对和只会更大，但当前 right 已是最右，故需增大左端
- `nums[left] + nums[right] > target`：`right--`

时间复杂度 $O(n)$，空间复杂度 $O(1)$，相比哈希表解法节省了 $O(n)$ 的额外空间。

例如，给定升序数组 `nums = [2, 7, 11, 15]`，目标值 `target = 9`：初始 `left=0, right=3`，`nums[0]+nums[3]=17>9`，右移 `right` 至 2；`nums[0]+nums[2]=13>9`，右移 `right` 至 1；`nums[0]+nums[1]=9==9`，返回 `[1, 2]`。整个过程仅执行 3 次比较，而暴力枚举需要 $\binom{4}{2}=6$ 次。

### 快慢指针（同向指针）

快慢指针中，快指针 `fast` 每步前进更快或负责"探路"，慢指针 `slow` 负责"记录"合法位置。两者始终同向移动，不会出现交叉。

**原地去重**是最典型的快慢指针应用：`slow` 指向已处理区间的末尾，`fast` 扫描新元素，当 `nums[fast] != nums[slow]` 时执行 `nums[++slow] = nums[fast]`。对于长度为 $n$ 的数组，`fast` 遍历一次即完成，时间复杂度严格为 $O(n)$，`slow` 最终指向去重后数组的最后一个有效元素的下标。

**链表找环**（Floyd 判圈算法）也属于快慢指针：令 `fast` 每次走 2 步，`slow` 每次走 1 步。若链表有环，`fast` 和 `slow` 必然在环内相遇。设链表头到环入口的距离为 $a$，环的长度为 $c$，环入口到相遇点的距离为 $b$，相遇时 `slow` 走了 $s = a + b$ 步，则 `fast` 走了 $2s = a + b + kc$（$k$ 为正整数）步，由此得 $s = kc$，即 $a + b = kc$，化简得 $a = (k-1)c + (c - b)$。这意味着：相遇后令一个指针回到头节点，另一个留在相遇点，两者以相同速度（每次1步）前进，它们再次相遇的位置恰好就是环入口节点。

**寻找链表中点**同样依赖快慢指针：`fast` 每次走 2 步，`slow` 每次走 1 步，当 `fast` 到达链表末尾时，`slow` 指向中间节点。对于长度为 $n$ 的链表，`slow` 恰好走了 $\lfloor n/2 \rfloor$ 步。这一技巧是归并排序链表版本（LeetCode 148）的关键子程序。

### 指针移动的合法性证明

双指针正确性的核心在于每次指针移动都是"无损"的——被跳过的候选组合可以被证明不满足条件。以三数之和（LeetCode 15）为例：外层固定一个数 `nums[i]`，内层用对撞指针在 `[i+1, n-1]` 区间寻找剩余两数。当遇到重复元素时需要跳过，因此循环中包含 `while (left < right && nums[left] == nums[left+1]) left++` 的去重逻辑，这一步骤不会导致遗漏，因为相同值的左指针产生的所有组合已经在第一次匹配时被记录。

## 关键公式与模型

### 时间复杂度汇总

设数组长度为 $n$，对撞指针的时间复杂度可以严格表达为：

$$T(n) = O(n), \quad S(n) = O(1)$$

对于三数之和（LeetCode 15），外层循环固定一个元素需要 $O(n)$，内层对撞指针为 $O(n)$，总体：

$$T(n) = O(n^2), \quad S(n) = O(1) \text{（不计输出空间）}$$

相比暴力枚举的 $O(n^3)$，节省了整整一个数量级。Floyd 判圈算法的时间复杂度为 $O(n)$，其中 $n$ 为链表节点总数，空间复杂度为 $O(1)$，而基于哈希集合的判环方法需要 $O(n)$ 的额外空间。

### 搜索空间缩减模型

对撞指针的搜索空间缩减模型：设每次迭代后搜索区间从 $[l, r]$ 变为 $[l+1, r]$ 或 $[l, r-1]$，区间长度严格减少 1，故最多经过 $n-1$ 次迭代必然终止。这一性质可以形式化为：

$$\sum_{k=0}^{n-1} 1 = n$$

即指针移动次数上界恰好为 $n$，是双指针 $O(n)$ 复杂度的严格证明基础。

### 快慢指针追及模型

设 `fast` 速度为 $v_f$，`slow` 速度为 $v_s$，且 $v_f > v_s$。进入环后，`fast` 相对 `slow` 的追及速度为 $v_f - v_s$，环长为 $c$，则最多经过：

$$t_{\max} = \left\lceil \frac{c}{v_f - v_s} \right\rceil$$

步后两者必然相遇。当 $v_f = 2, v_s = 1$ 时，$t_{\max} = c$，即最多绕环一圈即相遇。

### 容器盛水的容积公式

容器盛水问题（LeetCode 11）中，对撞指针每步选择移动较矮一侧。容积公式为：

$$\text{Volume}(l, r) = \min(\text{height}[l], \text{height}[r]) \times (r - l)$$

可以证明：若 $\text{height}[l] \leq \text{height}[r]$，则对所有 $r' < r$，均有：

$$\text{Volume}(l, r') \leq \text{height}[l] \times (r' - l) < \text{height}[l] \times (r - l) \leq \text{Volume}(l, r)$$

故移走左指针 $l$ 所对应的所有候选对均可安全跳过，贪心决策合法。

## 实际应用

### 经典算法题应用

**例如，容器盛水问题（LeetCode 11）**：给定 $n$ 个高度值组成的数组 `height`，需要找出两根柱子使其围成的容积最大。对撞指针从两端向内收缩，每次移动**较矮**一侧的指针。此贪心论证保证对撞指针在 $O(n)$ 内得出全局最优解，而暴力枚举需要 $O(n^2)$。以 `height = [1, 8, 6, 2, 5, 4, 8, 3, 7]` 为例，初始 `left=0（高度1）, right=8（高度7）`，容积为 $\min(1,7) \times 8 = 8$；移动左指针至 `left=1（高度8）`，容积为 $\min(8,7) \times 7 = 49$；继续收缩，最终全局最大容积为 $\min(8,7) \times 7 = 49$，整个过程仅需 8 次比较。

**三数之和（LeetCode 15）**：先对数组排序（$O(n \log n)$），外层枚举第一个数 `nums[i]`，内层对 `[i+1, n-1]` 使用对撞指针寻找两数之和等于 `-nums[i]` 的配对。整体时间复杂度 $O(n^2)$，比三重暴力循环的 $O(n^3)$ 快了一个数量级，且空间复杂度仅为 $O(1)$（不计排序递归栈）。

### AI 工程中的向量检索应用

**案例：AI 工程中的特征相似度计算**。在向量检索预处理阶段，对已按 L2 范数排序的候选向量列表，可用对撞指针快速过滤范数之和不满足阈值条件的向量对。设阈值为 $\theta$，若 $\|\mathbf{u}\|_2 + \|\mathbf{v}\|_2 < \theta$，则由三角不等式知 $\|\mathbf{u} - \mathbf{v}\|_2$ 不可能满足条件，可直接跳过。这将两两比较的 $O(n^2)$ 降为 $O(n)$，显著加速批量余弦相似度的候选集剪枝，在工业级 ANN（近似最近邻）检索系统中被广泛采用。

**滑动窗口作为双指针的推广**：当两个指针均从左端出发且右指针永不回头时，双指针退化为滑动窗口模式。例如，求最长无重复字符子串（LeetCode 3）：`right` 指针不断扩张窗口，一旦出现重复字符则 `left` 右移收