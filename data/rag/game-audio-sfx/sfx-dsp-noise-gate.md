---
id: "sfx-dsp-noise-gate"
concept: "噪声门"
domain: "game-audio-sfx"
subdomain: "dsp-effects"
subdomain_name: "混响与DSP"
difficulty: 3
is_milestone: false
tags: []

# Quality Metadata (Schema v2)
content_version: 3
quality_tier: "A"
quality_score: 76.3
generation_method: "intranet-llm-rewrite-v2"
unique_content_ratio: 1.0
last_scored: "2026-04-05"
sources:
  - type: "ai-generated"
    model: "mihoyo.claude-4-6-sonnet"
    prompt_version: "intranet-llm-rewrite-v2"
scorer_version: "scorer-v2.0"
quality_method: intranet-llm-rewrite-v2
updated_at: 2026-03-30
---

# 噪声门

## 概述

噪声门（Noise Gate）是一种基于幅度阈值的动态处理器，其工作逻辑与压缩器相反——当输入信号电平**低于**设定阈值时，噪声门将信号衰减至静默或极低电平；当信号超过阈值时，则允许信号完整通过。这种"开/关"式的控制机制使噪声门成为消除录音底噪、房间环境噪声以及游戏中持续性低电平噪声的首选工具。

噪声门的概念起源于1960年代的模拟录音工艺，最初由美国工程师Fil Orban等人将其商业化用于磁带录音降噪。在游戏音效工作流中，噪声门被广泛应用于运行时音频中间件（如Wwise、FMOD）的效果器链，用于动态切断玩家麦克风的环境底噪，以及在游戏内SFX中消除特定声音资产录制时遗留的本底噪声（通常为-60 dBFS至-40 dBFS区间的持续杂音）。

与简单的静音处理不同，噪声门通过参数化的时间控制实现自然过渡，避免声音的骤然切断，使处理后的音频在游戏音景中听起来仍然真实可信。

## 核心原理

### 四参数模型：阈值、起音、释音与保持

噪声门的完整行为由四个参数定义：

- **阈值（Threshold）**：单位为 dBFS，是门控的判断基准。信号电平高于此值时门"打开"，低于此值时门"关闭"。游戏音效中常见设置为 -40 dBFS 到 -20 dBFS。
- **起音（Attack）**：门从关闭状态切换至完全打开所需的时间，范围通常为 0.1 ms 至 10 ms。Attack 过快会导致瞬态出现咔嗒声，过慢则会削去声音的起始瞬态。
- **释音（Release）**：信号低于阈值后门完全关闭所需的时间，通常为 50 ms 至 500 ms。Release 过短会使门在词语间隙或击打声尾音中产生泵浦效果（pumping）。
- **保持（Hold）**：信号跌破阈值后门维持打开状态的最短时间，用于防止在信号短暂衰落时门快速反复开关造成的"颤动"（chattering）。

### 增益衰减量（Range）

除四参数外，许多专业噪声门插件还提供 **Range** 参数，决定门关闭时的最大衰减量（如 -80 dB），而非强制静音至负无穷。这在游戏中尤为实用：将 Range 设为 -30 dB 可保留轻微的环境感，避免声音在门关闭时产生不自然的"死寂"感，同时仍能有效压制底噪。

### 门控检测电路与RMS/峰值模式

噪声门内部的检测电路负责持续测量输入信号的电平。检测方式分为两种：
- **峰值（Peak）检测**：响应速度极快，适合控制瞬态丰富的鼓击声和枪声SFX。
- **RMS检测**：对信号电平进行时间平均，适合控制持续性声音如引擎环境音或玩家语音。

在Wwise的Noise Gate效果器中，这两种模式可通过参数直接切换，影响门的响应灵敏度。

## 实际应用

### 游戏语音聊天降噪

多人游戏中，玩家麦克风会持续拾取键盘噪声、风扇声等约 -50 dBFS 的环境底噪。将噪声门阈值设定在 -38 dBFS，Attack 设为 2 ms，Release 设为 150 ms，可在玩家停止说话后的150毫秒内平滑关闭麦克风信号，显著降低语音通道的背景噪声而不影响正常语音。

### 枪声SFX资产的尾音处理

在游戏音效制作中，录制的枪声样本往往含有长达300 ms的录音室房间反射尾音和设备底噪。通过在DAW（如Reaper或Pro Tools）中对每个样本应用噪声门，将阈值设在 -45 dBFS、Release 设为 80 ms，可精确截断枪声自然混响结束后的残余噪声，使样本在游戏引擎中循环或叠加时不会累积底噪。

### 侧链驱动的噪声门

利用侧链输入，噪声门可被外部信号触发而非被自身信号控制。典型用例：用玩家射击事件的触发信号（侧链）来控制环境音频通道的噪声门，使每次开枪时环境音短暂被压制，突出枪声SFX的冲击力。这正是侧链处理知识在噪声门中的直接延伸应用。

## 常见误区

### 误区一：阈值设得越高噪声消除越彻底

阈值过高会导致噪声门将有效信号的弱部分（如语音末尾的辅音、混响尾音）也一并切断。实际上，正确做法是将阈值设定在底噪电平以上约6 dB处，而非尽可能拉高。在游戏枪声处理中，若阈值高于 -30 dBFS，枪声的自然衰减尾音将被截断，产生明显的人工感。

### 误区二：噪声门可以替代录音时的降噪措施

噪声门只能在时间维度上分离噪声与信号，当底噪与有效信号同时存在时（如演讲中的风扇噪声），噪声门无法区分两者——门打开时噪声同样通过。真正的噪声抑制需要频谱降噪工具（如iZotope RX的Spectral De-noise），噪声门仅在信号间隙期间有效工作。

### 误区三：Release时间越短响应越准确

Release 过短（如低于 20 ms）会在信号的自然衰减阶段产生可闻的振幅截断，在游戏爆炸音的尾音中表现为明显的"咔"声。Release 需要根据被处理声音的自然混响时间（RT60）进行匹配，通常设定为该声音自然衰减时间的50%至70%。

## 知识关联

**前置概念——侧链处理**：理解噪声门需要掌握侧链检测电路的工作原理。侧链信号为噪声门提供独立的控制信号源，使门控判断逻辑与输出路由解耦，这是实现外部触发门控的基础机制。噪声门本质上是带有侧链检测能力的动态处理器。

**后续概念——声码器**：声码器（Vocoder）同样依赖信号的幅度包络提取与控制机制。声码器将一路信号（调制信号）的频段包络施加到另一路信号（载波信号）上，其每个频段的包络提取器在原理上与噪声门的检测电路共享相同的Attack/Release时间常数设计思路。掌握噪声门的时间参数行为，有助于理解声码器中多频段包络跟随器的调参逻辑。